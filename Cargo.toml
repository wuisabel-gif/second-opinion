[package]
name = "second-opinion"
version = "0.4.0"
edition = "2021"
description = "Agent-agnostic AI pull request reviews as a GitHub Action"
license = "MIT"
repository = "https://github.com/wuisabel-gif/second-opinion"
readme = "README.md"

[dependencies]
anyhow = "1"
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
ureq = { version = "=2.9.7", features = ["json"] }
url = "2"

[profile.release]
opt-level = 1
